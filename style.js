import { StyleSheet } from 'react-native';
const styles = StyleSheet.create({
    container: {
        flex: 1, marginTop: 50
    },
    sep: { height: 1, backgroundColor: 'red' },
    drawerItem: {
        paddingLeft: 30,
        height: 50,
        justifyContent: 'center'
      }
}
);
export default styles;